<resources>

    <!-- Base application theme. -->
    <style name="Theme.DesignDemo" parent="Base.Theme.AppCompat.Light">
        <item name="android:fontFamily">@font/hekaya</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="Base.Theme.DesignDemo" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">#FF4081</item>
        <item name="android:textColor">#000</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="DrawerTextStyle" parent="Theme.MaterialComponents">
        <item name="android:fontFamily">@font/naskh</item>
        <item name="android:textSize">18sp</item>
    </style>
    <style name="toolbar_style" parent="Base.Theme.AppCompat">
        <item name="android:fontFamily">@font/kafa</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
