<resources>

    <!-- Base application theme. -->
    <style name="Theme.DesignDemo" parent="Base.Theme.MaterialComponents">
        <item name="android:fontFamily">@font/mitra</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="Base.Theme.DesignDemo" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">#FF4081</item>
        <item name="android:textColor">#000</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="DrawerTextStyle" parent="Theme.MaterialComponents">
        <item name="android:fontFamily">@font/farasha_medium</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
